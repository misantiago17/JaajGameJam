using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CheckListManager : MonoBehaviour
{

    // Verifica se tem opção disponível, se tiver ele aciona a base do checklist e os textos da checklist - fazer um Update disso toda vez que um evento triggar algo da checklist
    // Toda action Trigger tem um botão de bool perguntando se aciona alguma coisa da checklist ou não
    // Existem duas opções, acionar um item na checklist ou 
    // Fuck isso vai ser impossível



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
